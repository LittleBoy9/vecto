{
  "$schema": "https://schema.tauri.app/config/2/capability.json",
  "identifier": "default",
  "description": "Default capability set for Vecto. Only native file dialogs and the auto-updater are exposed to the webview — all filesystem access goes through the open_svg_file / save_svg_file commands, so the fs and shell plugin permissions are deliberately not granted.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "updater:default",
    "process:allow-restart"
  ]
}
